# Data caches — regenerated automatically
*_cache.json
*_cache_*.json
sleeper_stats_cache*.json

# User data / output
draft_selections.json
draft_selections_*.json
my_team.json
*.xlsx

# OS / editor
.DS_Store
~$*
favorites.json

# Unrelated projects / local tooling / debug artifacts (kept on disk, not in repo)
pi-dashboard/
.claude/
*.docx

# Local archive of superseded files — never tracked
workings/
